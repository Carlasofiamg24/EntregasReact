/* import React from "react";
import ImgPelu from "./ImgPelu";

const ImageListPelu = ({items}) => {
    return(
        <div className="container">
            <div className="row">
            {items.map((item)=>{
                return <div className="col-md-12 d-flex justiy-content-center" key={item.id}><ImgPelu key={item.id} item={item} /></div>
            })}
            </div>
        </div>
    )
};

export default ImageListPelu; */